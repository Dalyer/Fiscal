# Main.py

"""This is where the magic happens. Should import pdftotree and learn how to work that package."""

import Transaction
import Category
import pdftotree

pdf_file = "placeholder"

# pdftotree.parse(pdf_file, html_path=None, model_type=None, model_path=None, favor_figures=True, visualize=False)
